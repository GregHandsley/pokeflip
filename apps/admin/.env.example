# ============================================
# Environment Configuration Example
# ============================================
# Copy this file to .env.local (for local development)
# or set these variables in your deployment environment
#
# Note: Variables prefixed with NEXT_PUBLIC_ are exposed to the client
#       Variables without this prefix are server-only and more secure
# ============================================

# ============================================
# Application Environment
# ============================================
# Set to "development", "staging", or "production"
# If not set, defaults to NODE_ENV
APP_ENV=development

# ============================================
# Supabase Configuration (REQUIRED)
# ============================================
# Get these from your Supabase project dashboard:
# https://app.supabase.com/project/YOUR_PROJECT/settings/api

# Supabase project URL (public - exposed to client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key (public - exposed to client, safe for client-side use)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (PRIVATE - server-only, never expose to client!)
# This key bypasses Row Level Security - handle with extreme care
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# Sentry Error Tracking (OPTIONAL)
# ============================================
# Get DSN from: https://sentry.io/settings/YOUR_ORG/projects/YOUR_PROJECT/keys/

# Sentry DSN (can be public or private, both work)
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# OR use private server-only variable:
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Enable Sentry in development/staging (default: only enabled in production)
# Set to "true" to enable in all environments
NEXT_PUBLIC_SENTRY_ENABLED=false

# Sentry organization and project (required for source map uploads in builds)
SENTRY_ORG=your-org-slug
SENTRY_PROJECT=your-project-slug

# ============================================
# Application Configuration (OPTIONAL)
# ============================================

# Site URL (used for CORS, absolute URLs, etc.)
# Defaults to http://127.0.0.1:3000 in development
NEXT_PUBLIC_SITE_URL=http://127.0.0.1:3000

# Minimum price floor for card listings (in GBP)
# Defaults to 0.99 if not set
PRICE_FLOOR_GBP=0.99

# ============================================
# Build/Runtime Configuration (AUTOMATIC)
# ============================================
# These are typically set automatically by the framework or CI/CD
# You usually don't need to set these manually

# Node environment (set automatically by Next.js)
# NODE_ENV=development

# Next.js runtime (set automatically)
# NEXT_RUNTIME=nodejs

# CI environment flag (set by CI/CD systems)
# CI=true
