/**
 * Default card back image to use when a card image is missing
 * Place the card back image at: apps/admin/public/card-back-default.png
 */
export const DEFAULT_CARD_BACK_IMAGE = "/card-back-default.png";

/**
 * Default Pokéball image to use when a set image is missing
 * Place the Pokéball image at: apps/admin/public/pokeball-default.png
 */
export const DEFAULT_SET_IMAGE = "/pokeball-default.png";
